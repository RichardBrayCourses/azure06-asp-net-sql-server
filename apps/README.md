# Apps

Independently deployable React applications live here.

Current:

- `shell` - the existing React/Vite application, renamed from the earlier single frontend app.

Future bounded apps can be added as real deployable frontends when their route ownership is extracted, for example `cases-web`, `people-web`, and `admin-web`.
